<UserControl x:Class="ReplaysApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:ReplaysApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="{StaticResource PrimaryBackgroundColor}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Lance Certo" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            
            <Label Content="Usuário:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/>
            
            <Label Content="Senha:" Margin="0,15,0,0"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,5,0,0"/>

            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,20,0,0" Padding="10,10" FontWeight="Bold"/>
            <Button Content="Registrar" Command="{Binding RegisterCommand}" Margin="0,10,0,0" Padding="10,10"/>
        </StackPanel>
    </Grid>
</UserControl>